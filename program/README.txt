Milestone 4:
for this milestone progrom. we have three different part of program. 
main.m: it is script file for the octave / matlab. It is like main function for the java. it will call other two function for this half program. And then show some result. it will has two different cell array to save two group of data get from userinput from getInput function.
then it will run the class_average function to get class average.

getInput.m : this function is get all student name and their grade from user input. it will save those two group of data into two different cell arraies.

class_average.m: it will ask user to get average score of class or not. If user input 'y' it will show the average of students grade for this class.
if user press 'n' it will nothing happend.(it will show next milesteon).
